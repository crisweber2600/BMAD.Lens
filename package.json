{
  "private": true,
  "workspaces": [
    "src/modules/lens-work",
    "src/modules/file-transforms"
  ],
  "dependencies": {
    "bmad-method": "^6.0.0-Beta.7"
  },
  "scripts": {
    "build": "echo 'No build step needed — asset-only packages'",
    "test": "npm test --workspaces --if-present",
    "version:alpha": "npm version prerelease --preid=alpha --workspaces",
    "publish:dry": "npm publish --dry-run --workspaces --access public",
    "publish": "npm publish --workspaces --access public"
  }
}

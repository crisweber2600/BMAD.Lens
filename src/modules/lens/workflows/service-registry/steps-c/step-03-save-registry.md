---
name: 'step-03-save-registry'
description: 'Persist registry changes'
---

# Step 3: Save Registry

## Goal
Persist the registry to disk.

## Instructions
- Create `.lens/` if missing.
- Write `.lens/service-registry.yaml`.

## Output
- `.lens/service-registry.yaml`

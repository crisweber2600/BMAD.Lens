---
name: 'step-01-load-registry'
description: 'Load service registry'
nextStepFile: './step-02-edit-registry.md'
---

# Step 1: Load Registry

## Goal
Load the service registry.

## Instructions
- Read `.lens/service-registry.yaml` if present.
- If missing, initialize an empty registry structure.

## Output
- `service_registry`

---
description: Manage service registry mappings and aliases
---

Activate Navigator agent and execute service-registry:

1. Load agent: `_bmad/lens/agents/navigator.agent.yaml`
2. Execute `service registry` command to edit service mappings
3. Save and confirm registry updates

Use `#think` before changing identifiers or alias mappings.

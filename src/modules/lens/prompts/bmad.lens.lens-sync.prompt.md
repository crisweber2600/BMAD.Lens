---
description: Reconcile architectural drift between documentation and code
---

Activate Navigator agent and execute lens-sync:

1. Load agent: `_bmad/lens/agents/navigator.agent.yaml`
2. Execute `lens sync` command to discover and reconcile drift
3. Present the reconciliation plan and apply updates

Use `#think` before applying structural changes or conflict resolutions.

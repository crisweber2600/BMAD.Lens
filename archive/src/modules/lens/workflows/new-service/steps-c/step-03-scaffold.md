---
name: 'step-03-scaffold'
description: 'Scaffold service structure'
---

# Step 3: Scaffold

## Goal
Create a basic service structure if applicable.

## Instructions
- If `services/` exists, create `services/{service_name}`.
- If `apps/` exists, create `apps/{service_name}`.
- Save updated domain map to `.lens/domain-map.yaml`.

## Output
- Service folder created (if applicable)
- `.lens/domain-map.yaml` updated

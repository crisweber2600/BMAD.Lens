---
description: Restore the previous lens session and context
---

Activate Navigator agent and execute lens-restore:

1. Load agent: `_bmad/lens/agents/navigator.agent.yaml`
2. Execute `restore session` command to load the last session
3. Validate and rehydrate the lens context

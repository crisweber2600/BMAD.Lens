---
description: Create new microservice (bounded context) in service
---

Activate Navigator agent and execute new-microservice:

1. Load agent: `_bmad/lens/agents/navigator.agent.yaml`
2. Execute `new microservice` command to create a new microservice

Use `#think` before defining microservice boundaries or dependencies.

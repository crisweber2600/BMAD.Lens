---
description: Detect current architectural lens and load summary
---

Activate Navigator agent and execute lens-detect:

1. Load agent: `_bmad/lens/agents/navigator.agent.yaml`
2. Execute `nav` (lens-detect) command to detect the current lens
3. Present the summary and persist the session snapshot

```prompt
---
description: "[DEPRECATED] Story generation is now part of /devproposal phase"
---

**This command has been superseded by `/devproposal`.**

The story generation workflow is now part of the DevProposal phase (audience: medium).

Use `/devproposal` instead, which includes:
- Epic creation
- Story generation with acceptance criteria
- Implementation readiness checklist

**Lifecycle v2 phase chain:**
preplan → businessplan → techplan → [small→medium] → devproposal → [medium→large] → sprintplan → [large→base] → dev

```

---
description: Create new logical service structure in domain map
---

Activate Navigator agent and execute new-service:

1. Load agent: `_bmad/lens/agents/navigator.agent.yaml`
2. Execute `new service` command to create a new service structure

Use `#think` before proposing service boundaries or naming conventions.

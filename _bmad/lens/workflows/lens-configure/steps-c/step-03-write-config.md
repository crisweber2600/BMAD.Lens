---
name: 'step-03-write-config'
description: 'Write .lens/lens-config.yaml'
---

# Step 3: Write Config

## Goal
Persist the configuration to the project.

## Instructions
- Create `.lens/` folder if missing.
- Write `lens-config.yaml` using validated settings.
- Confirm the saved path to the user.

## Output
- `.lens/lens-config.yaml`
